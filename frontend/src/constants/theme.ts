/** Spinner color on primary buttons (matches primary-foreground). */
export const PRIMARY_BUTTON_SPINNER_COLOR = '#fafafa';

/** App always renders in dark mode (see root layout `className="dark"`). */
export const APP_COLOR_SCHEME = 'dark' as const;

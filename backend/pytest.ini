[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
pythonpath = .
testpaths = tests
markers =
    smoke: live API smoke tests (require KEEPALIVE_* env vars; excluded from default CI)

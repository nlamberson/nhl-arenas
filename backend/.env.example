# Copy this file to .env and update with your values
# Command: cp .env.example .env

# Application Settings
APP_NAME=NHL Arena Tracker API
ENVIRONMENT=development
LOG_LEVEL=INFO

# CORS Origins - comma-separated list, or "*" for all origins (dev only!)
# Examples:
#   Development: CORS_ORIGINS=*
#   Production:  CORS_ORIGINS=https://myapp.com,https://www.myapp.com
CORS_ORIGINS=*

# Firebase Configuration
# Get this from Firebase Console -> Project Settings -> Project ID
FIREBASE_PROJECT_ID=<project-id>

# ============================================================================
# Firebase Service Account - Choose ONE option based on your deployment:
# ============================================================================

# OPTION 1: File path (Local development & Docker)
# Download from: Firebase Console -> Project Settings -> Service accounts -> Generate new private key
GOOGLE_APPLICATION_CREDENTIALS=./firebase-service-account.json

# OPTION 2: Base64-encoded JSON (Cloud platforms like Railway, Render, Fly.io)
# Generate with: cat firebase-service-account.json | base64
# FIREBASE_SERVICE_ACCOUNT_BASE64=<base64-encoded-json>

# OPTION 3: Google Cloud (Cloud Run, GKE, etc.)
# No configuration needed! ADC is automatic. Just ensure the service account
# attached to your Cloud Run service has Firebase Admin permissions.

# ============================================================================
# Database Configuration - Choose ONE option:
# ============================================================================

# OPTION 1 (Recommended): Individual components
# The backend will auto-construct: postgresql+psycopg://user:pass@host:port/db
POSTGRES_USER=<user>
POSTGRES_PASSWORD=<password>
POSTGRES_DB=nhl_arenas
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# OPTION 2 (Alternative): Full URL
# Good for cloud providers (Railway, Render, etc.) - uncomment to use:
# DATABASE_URL=postgresql+psycopg://<user>:<password>@localhost:5432/nhl_arenas

# Note: If DATABASE_URL is set, it takes priority over individual components
